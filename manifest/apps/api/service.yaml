apiVersion: v1
kind: Service
metadata:
  name: golang-service
  namespace: apps
spec:
  selector:
    app: go-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
